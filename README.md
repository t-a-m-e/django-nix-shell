This project is simply how to create a nix shell with the Django framework included in the derivation. That is the .nix file. The .odp file is the presentation that walks through the process of getting a
simple website up and running using Django. 
